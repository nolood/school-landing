import React from 'react';
import { LoginForm } from '@/widgets/auth-form/ui';

const LoginPage = () => {
  return (
    <section className='section'>
      <div className='container'>
        <LoginForm />
      </div>
    </section>
  );
};

export default LoginPage;
